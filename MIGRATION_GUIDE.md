# 📚 Руководство по Миграции Истории Чата

## Проблема

Telegram Bot API **не предоставляет** прямого доступа к истории чата. Бот может видеть только:
- Сообщения, отправленные после добавления бота в чат
- Сообщения, где бот упомянут (@bot)
- Команды, адресованные боту

**НО:** Полная история чата (все сообщения до момента добавления бота) недоступна через Bot API! 😔

---

## ✅ Решение: 3 Способа Загрузить Историю

### Способ 1: Telegram Desktop Export (Самый Простой)

**Время:** 10-30 минут в зависимости от размера истории

#### Шаг 1: Экспорт из Telegram Desktop

1. Откройте **Telegram Desktop** (не web, не mobile!)
2. Откройте нужный чат
3. Нажмите **⋮** (три точки) → **Export chat history**
4. Настройте параметры:
   ```
   ✅ Format: JSON
   ❌ Photos: NO
   ❌ Videos: NO
   ❌ Voice messages: NO
   ✅ Message range: All time
   ```
5. Нажмите **Export**
6. Дождитесь окончания (файл `result.json`)

#### Шаг 2: Импорт в Бота

```bash
# 1. Скопируйте result.json в папку с проектом
cp ~/Downloads/ChatExport_*/result.json /path/to/project/

# 2. Проверьте файл (dry run)
go run scripts/import_telegram_export.go -file=result.json -dry-run

# 3. Если все ОК - импортируйте
go run scripts/import_telegram_export.go -file=result.json
```

**Что произойдет:**
- Все текстовые сообщения загрузятся в `chat_messages`
- Автоматически сгенерируются embeddings
- RAG будет доступен для всей истории

**Время:**
- 1,000 сообщений: ~2-5 минут
- 10,000 сообщений: ~20-30 минут
- 100,000 сообщений: ~3-5 часов

---

### Способ 2: Python Script с MTProto API (Продвинутый)

**Для кого:** Если нужна полная история программным способом

#### Требования

```bash
pip install telethon
```

#### Получение API Credentials

1. Зайдите на https://my.telegram.org/auth
2. Войдите в аккаунт
3. Перейдите в "API development tools"
4. Получите `api_id` и `api_hash`

#### Скрипт для Загрузки

```python
# download_history.py
from telethon.sync import TelegramClient
import json

# Ваши credentials
api_id = 12345  # Замените на свой
api_hash = 'your_api_hash_here'  # Замените на свой

# ID чата (можно узнать через @userinfobot)
chat_id = -1001234567890

# Создание клиента
client = TelegramClient('session_name', api_id, api_hash)

async def download_messages():
    await client.start()
    
    messages = []
    count = 0
    
    print("Загружаю историю...")
    
    async for message in client.iter_messages(chat_id, limit=None):
        if message.text:
            messages.append({
                'id': message.id,
                'type': 'message',
                'date': message.date.isoformat(),
                'date_unixtime': str(int(message.date.timestamp())),
                'from': message.sender.first_name if message.sender else 'Unknown',
                'from_id': f"user{message.sender_id}" if message.sender_id else '',
                'text': message.text
            })
            
            count += 1
            if count % 1000 == 0:
                print(f"Загружено {count} сообщений...")
    
    # Сохранение в JSON
    export_data = {
        'name': 'Chat History',
        'type': 'group_chat',
        'id': chat_id,
        'messages': list(reversed(messages))  # От старых к новым
    }
    
    with open('history.json', 'w', encoding='utf-8') as f:
        json.dump(export_data, f, ensure_ascii=False, indent=2)
    
    print(f"✅ Готово! Сохранено {len(messages)} сообщений в history.json")

# Запуск
with client:
    client.loop.run_until_complete(download_messages())
```

#### Использование

```bash
python3 download_history.py
# После завершения:
go run scripts/import_telegram_export.go -file=history.json
```

---

### Способ 3: Автоматическое Накопление (Без Истории)

**Для кого:** Если старая история не критична

Просто оставьте бота работать - он автоматически сохраняет **все новые сообщения**.

**Плюсы:**
- Не требует никаких действий
- Работает автоматически
- История накапливается естественно

**Минусы:**
- Нет старых сообщений
- Нужно ждать накопления

---

## 🚀 Быстрый Старт

### Вариант A: У вас есть Telegram Desktop

```bash
# 1. Экспортируйте чат (см. Способ 1)

# 2. Импортируйте
go run scripts/import_telegram_export.go -file=result.json

# 3. Проверьте в Supabase
# SELECT * FROM rag_statistics;

# 4. Готово! Используйте бота с полной историей
```

### Вариант B: Нужен программный доступ

```bash
# 1. Установите Telethon
pip install telethon

# 2. Создайте скрипт (см. Способ 2)

# 3. Запустите
python3 download_history.py

# 4. Импортируйте
go run scripts/import_telegram_export.go -file=history.json

# 5. Готово!
```

### Вариант C: Не нужна старая история

```bash
# Ничего не делайте 😊
# Бот автоматически сохраняет все новые сообщения
```

---

## 📊 Проверка Результатов

### В Telegram

```
/migrate_history - Показать инструкцию
/sync - Проиндексировать загруженные сообщения
/help - Проверить что RAG включен
```

### В Supabase SQL Editor

```sql
-- Общая статистика
SELECT * FROM rag_statistics;

-- Последние сообщения
SELECT 
    id, 
    username, 
    LEFT(message_text, 50) as preview,
    indexed,
    created_at
FROM chat_messages
ORDER BY created_at DESC
LIMIT 10;

-- Проверка embeddings
SELECT 
    COUNT(*) as total,
    COUNT(CASE WHEN indexed = true THEN 1 END) as with_embeddings,
    COUNT(CASE WHEN indexed = false THEN 1 END) as pending
FROM chat_messages;
```

---

## ⚠️ Важные Замечания

### Ограничения Telegram Bot API

- **НЕ МОЖЕТ** получить историю до добавления бота
- **НЕ МОЖЕТ** читать все сообщения без упоминания (в некоторых типах чатов)
- **МОЖЕТ** видеть только новые сообщения

Поэтому для **полной истории** нужен MTProto API (через Telethon/TDLib) или Telegram Desktop экспорт.

### Rate Limits

**Gemini Embeddings API:**
- ~1500 запросов/минуту
- Скрипт импорта автоматически учитывает лимиты

### Размер Данных

**Примерный объем для 100k сообщений:**
- Текст: ~100 MB
- Embeddings: ~300 MB (768 dimensions × 4 bytes × 100k)
- Индексы: ~200 MB

**Итого:** ~600 MB для 100k сообщений

---

## 🐛 Troubleshooting

### "Failed to parse export JSON"

**Причина:** Неправильный формат файла

**Решение:**
```bash
# Проверьте формат
head -n 20 result.json

# Должно быть:
{
  "name": "...",
  "type": "...",
  "id": ...,
  "messages": [...]
}
```

### "Rate limit exceeded"

**Причина:** Gemini API rate limit

**Решение:**
- Подождите 1 минуту
- Запустите скрипт снова (он продолжит с места остановки)

### Импорт идет слишком долго

**Нормально!** Для 10k сообщений = ~30 минут

**Можно:**
- Запустить ночью
- Разбить на части:
  ```bash
  # Первая 1000
  jq '.messages = .messages[:1000]' result.json > batch1.json
  go run scripts/import_telegram_export.go -file=batch1.json
  
  # Вторая 1000
  jq '.messages = .messages[1000:2000]' result.json > batch2.json
  go run scripts/import_telegram_export.go -file=batch2.json
  ```

---

## ✅ Checklist

После импорта проверьте:

- [ ] Сообщения появились в `chat_messages`
- [ ] `indexed = true` для всех сообщений
- [ ] `rag_statistics` показывает 100% indexed
- [ ] Бот отвечает с учетом контекста истории
- [ ] `/sync` больше не находит неиндексированных сообщений

---

## 🎯 Что Дальше?

1. **Обновление истории:**
   - Бот автоматически сохраняет все новые сообщения
   - Ночью (03:00 MSK) запускается автоматическая индексация
   - Можно вручную: `/sync`

2. **Мониторинг:**
   ```sql
   SELECT * FROM rag_statistics;
   SELECT * FROM daily_message_stats LIMIT 7;
   ```

3. **Использование:**
   - Упоминайте бота: `@bot как мы решали вопрос X?`
   - Бот автоматически ищет релевантные сообщения из истории
   - Отвечает с учетом контекста

---

**Создано:** 2025-11-20  
**Версия:** 1.0  
**Автор:** Cursor AI Agent
