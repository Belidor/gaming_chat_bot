# Getting Started - Telegram LLM Bot

## 🎯 Пошаговая Инструкция для Первого Запуска

### Шаг 1: Создание Telegram Бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, "My LLM Assistant")
   - Введите username бота (например, "my_llm_bot")
4. Скопируйте токен (например, `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. **Сохраните**: 
   - Token: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
   - Username: `my_llm_bot`

### Шаг 2: Добавление Бота в Группу и Получение Chat ID

1. Создайте групповой чат в Telegram
2. Добавьте туда своего бота (найдите по username)
3. Отправьте любое сообщение в группу
4. Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
   Замените `<YOUR_BOT_TOKEN>` на ваш токен
5. Найдите в ответе:
   ```json
   "chat": {
     "id": -1001234567890,
     "title": "My Group",
     "type": "supergroup"
   }
   ```
6. **Сохраните** Chat ID: `-1001234567890`

### Шаг 3: Получение Gemini API Key

1. Откройте [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите с Google аккаунтом
3. Нажмите "Create API Key"
4. **Сохраните** API Key: `AIzaSy...`

### Шаг 4: Создание Supabase Проекта

1. Откройте [Supabase](https://supabase.com)
2. Зарегистрируйтесь/войдите
3. Нажмите "New Project"
4. Заполните:
   - Name: `telegram-llm-bot`
   - Database Password: (придумайте надежный пароль)
   - Region: выберите ближайший
5. Дождитесь создания проекта (~2 минуты)
6. Перейдите в Settings → API
7. **Сохраните**:
   - URL: `https://xxxxxxxxxxxxx.supabase.co`
   - anon/public key: `eyJhbGciOiJ...`

### Шаг 5: Создание Базы Данных

1. В проекте Supabase откройте "SQL Editor"
2. Нажмите "New Query"
3. Откройте файл `deployments/supabase/schema.sql` из проекта
4. Скопируйте весь содержимое и вставьте в SQL Editor
5. Нажмите "Run" (или Ctrl+Enter)
6. Убедитесь что выполнилось без ошибок
7. Проверьте что созданы:
   - Таблицы: `request_logs`, `daily_limits`
   - Функции: `get_daily_limit`, `increment_daily_limit`
   - View: `daily_statistics`

### Шаг 6: Настройка Environment Variables

1. Скопируйте пример файла:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и заполните:
   ```env
   # Telegram Bot (из Шага 1 и 2)
   TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   TELEGRAM_BOT_USERNAME=my_llm_bot
   TELEGRAM_GROUP_CHAT_ID=-1001234567890

   # Google Gemini API (из Шага 3)
   GEMINI_API_KEY=AIzaSy...

   # Supabase (из Шага 4)
   SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
   SUPABASE_KEY=eyJhbGciOiJ...

   # Остальное можно оставить по умолчанию
   TIMEZONE=Europe/Moscow
   LOG_LEVEL=info
   ENVIRONMENT=production
   PRO_DAILY_LIMIT=5
   FLASH_DAILY_LIMIT=25
   MAX_RESPONSE_LENGTH=5000
   GEMINI_TIMEOUT=30
   SUPABASE_TIMEOUT=10
   ```

3. Сохраните файл

### Шаг 7: Запуск Бота

#### Вариант A: Docker Compose (Рекомендуется)

```bash
# Убедитесь что Docker установлен
docker --version
docker-compose --version

# Запустите бота
docker-compose up -d

# Просмотрите логи
docker-compose logs -f

# Вы должны увидеть:
# - "Starting Telegram LLM Bot"
# - "Supabase connection successful"
# - "Bot initialized successfully"
# - "Bot is running"
```

#### Вариант B: Локальный Запуск

```bash
# Убедитесь что Go установлен
go version  # Должно быть 1.21+

# Скачайте зависимости
go mod download

# Запустите бота
go run cmd/bot/main.go

# Или соберите и запустите
go build -o telegram-llm-bot cmd/bot/main.go
./telegram-llm-bot
```

### Шаг 8: Тестирование

1. Откройте групповой чат где добавлен бот
2. Отправьте сообщение:
   ```
   @my_llm_bot привет, как дела?
   ```
3. Бот должен ответить через несколько секунд
4. Проверьте статистику:
   ```
   /stats
   ```
5. Должны увидеть:
   ```
   📊 Статистика для [Ваше Имя]
   🤖 Gemini Pro: Использовано: 1/5, Осталось: 4
   ⚡ Gemini Flash: Использовано: 0/25, Осталось: 25
   📈 Всего запросов: 1
   ⏰ Сброс лимитов через: X ч.
   ```

### Шаг 9: Проверка Логов в Supabase

1. Откройте Supabase проект
2. Перейдите в "Table Editor"
3. Выберите таблицу `request_logs`
4. Должны увидеть ваш запрос с ответом
5. Проверьте таблицу `daily_limits`
6. Должна быть запись с вашим user_id и счетчиками

---

## 🎉 Готово!

Бот запущен и работает! Теперь вы можете:

- Задавать вопросы через @mention
- Проверять статистику через /stats
- Смотреть логи в Supabase
- Мониторить использование через Supabase Dashboard

---

## 🛠 Управление Ботом

### Остановить бота

```bash
# Docker
docker-compose down

# Локально
# Нажмите Ctrl+C
```

### Перезапустить бота

```bash
# Docker
docker-compose restart

# Или полная пересборка
docker-compose down
docker-compose up -d --build
```

### Просмотреть логи

```bash
# Docker - в реальном времени
docker-compose logs -f

# Docker - последние 100 строк
docker-compose logs --tail=100

# Docker - только ошибки
docker-compose logs | grep ERROR
```

### Обновить конфигурацию

1. Отредактируйте `.env`
2. Перезапустите бота:
   ```bash
   docker-compose restart
   ```

---

## ❓ Частые Проблемы

### Бот не отвечает на сообщения

**Проблема**: Отправляю @mention, но бот молчит

**Решение**:
1. Проверьте что бот запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs -f`
3. Убедитесь что `TELEGRAM_GROUP_CHAT_ID` правильный
4. Проверьте что бот добавлен в группу
5. Попробуйте удалить бота из группы и добавить заново

### Ошибка "Supabase ping failed"

**Проблема**: Бот не может подключиться к Supabase

**Решение**:
1. Проверьте `SUPABASE_URL` и `SUPABASE_KEY` в `.env`
2. Убедитесь что проект Supabase активен
3. Проверьте интернет соединение
4. Попробуйте пересоздать API ключ в Supabase

### Ошибка "Failed to create genai client"

**Проблема**: Не удается подключиться к Gemini API

**Решение**:
1. Проверьте `GEMINI_API_KEY` в `.env`
2. Убедитесь что у вас есть квота API
3. Проверьте что ключ активен в Google AI Studio
4. Попробуйте создать новый API ключ

### "PGRST116" или "Function not found"

**Проблема**: Ошибки при работе с БД

**Решение**:
1. Убедитесь что выполнен `schema.sql` полностью
2. Проверьте в Supabase SQL Editor:
   ```sql
   SELECT * FROM pg_proc WHERE proname LIKE '%daily_limit%';
   ```
3. Если функций нет - выполните schema.sql заново

### Бот не сбрасывает лимиты в полночь

**Проблема**: Лимиты не обнуляются

**Решение**:
1. Проверьте `TIMEZONE` в `.env` (должно быть `Europe/Moscow`)
2. Проверьте системное время в контейнере:
   ```bash
   docker exec telegram-llm-bot date
   ```
3. Проверьте что дата в таблице `daily_limits` в правильном формате

---

## 📈 Мониторинг

### Просмотр Статистики в Supabase

1. Откройте Supabase → SQL Editor
2. Выполните запросы:

```sql
-- Общая статистика за сегодня
SELECT * FROM daily_statistics 
WHERE date = CURRENT_DATE;

-- Топ пользователей
SELECT 
    username,
    COUNT(*) as total_requests,
    COUNT(DISTINCT DATE(created_at)) as active_days
FROM request_logs
GROUP BY username
ORDER BY total_requests DESC
LIMIT 10;

-- Использование моделей
SELECT 
    model_used,
    COUNT(*) as requests,
    AVG(execution_time_ms) as avg_time,
    AVG(response_length) as avg_length
FROM request_logs
WHERE created_at > NOW() - INTERVAL '7 days'
GROUP BY model_used;

-- Ошибки за последний час
SELECT *
FROM request_logs
WHERE error_message IS NOT NULL 
AND created_at > NOW() - INTERVAL '1 hour'
ORDER BY created_at DESC;
```

---

## 🔐 Безопасность

### Важно!

1. **Никогда не коммитьте .env файл в git**
2. **Используйте надежные пароли для Supabase**
3. **Ограничьте доступ к API ключам**
4. **Регулярно проверяйте логи на подозрительную активность**
5. **Не публикуйте токены в открытом доступе**

### Рекомендации

- Используйте Supabase service key (не anon) для production
- Настройте Row Level Security в Supabase
- Ограничьте квоту Gemini API
- Регулярно меняйте API ключи
- Настройте алерты в Supabase на необычную активность

---

## 📚 Дополнительная Информация

- **README.md** - Полная документация
- **SPECIFICATION.md** - Техническая спецификация
- **PROJECT_SUMMARY.md** - Обзор проекта

---

**Удачи с использованием бота! 🚀**

Если возникнут вопросы - обращайтесь к документации или проверяйте логи.
